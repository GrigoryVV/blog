{
  "name": "blog",
  "private": true,
  "scripts": {
    "backend:dev": "lerna exec --scope backend -- yarn develop",
    "frontend:dev": "lerna exec --scope frontend -- yarn dev"
  },
  "devDependencies": {
    "lerna": "^4.0.0"
  }
}
